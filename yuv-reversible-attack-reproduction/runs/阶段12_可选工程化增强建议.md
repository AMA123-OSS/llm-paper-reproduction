# 阶段 12：可选工程化增强建议

日期：2026-06-09

## 当前已完成的低风险增强

- 已新增 `runs\README.md`，说明实验记录规则、关键结果表和脚本。
- 已新增多阶段独立评估脚本，能够生成指标表和汇总表。
- 阶段 4-10 均避免直接修改根目录主算法，降低破坏原始行为的风险。

## 暂不直接改主算法的原因

当前用户重点是论文理解、复现实验结果和 Paper 信息整理。主脚本参数化、恢复脚本闭环、requirements 固化都值得做，但属于下一轮工程改造。如果现在直接重构 `reversible_attack_OURS.py`，可能引入新变量，影响已经完成的阶段 4-10 结果可比性。

## 下一轮建议优先级

1. 增加恢复元数据 sidecar 和 `recover_from_rae.py`，优先补齐 error-free recovery 验证。
2. 给主脚本增加 CLI 参数：`--input-dir`、`--output-dir`、`--steps`、`--max-iteration`、`--device`。
3. 增加 `requirements.txt`，并单独说明 PyTorch CUDA wheel 安装索引。
4. 增加 `scripts\smoke_test.ps1`，一键运行单图冒烟测试。
5. 将阶段 6-10 的评估脚本迁移到正式 `scripts` 目录。
6. 固化 `.gitignore`，忽略 `.venv`、`wheelhouse`、模型缓存和大体积输出图。

## 阶段 12 状态

阶段 12 本轮仅完成记录体系增强和建议整理，没有直接重构主算法。

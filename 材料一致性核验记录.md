# 材料一致性核验记录：YUV 可逆对抗攻击

核验日期：2026-06-09  
工作目录：`D:\大模型论文复现`  
当前主线：`Efficient and Transferable Reversible Adversarial Attacks Utilizing YUV Color Space`

## 1. 核验结论

- [x] 当前项目主线与 `最后确认文件.md` 一致。
- [x] 论文来源与殷赵霞老师 publications 页面一致。
- [x] DOI 与 ScienceDirect 页面一致：`10.1016/j.neucom.2025.131088`。
- [x] 代码仓库与 publications 页面代码链接一致。
- [x] GitHub 远程仓库结构与 `架构概览.md` 中的模块划分一致。
- [x] 当前本地已存在 YUV 项目代码目录 `YUV_Reversible_Attack_2025`，无 `.git`，推测为 ZIP/复制方式获取。
- [x] 旧 Rt-LRM/LCO 目录和文档属于历史候选材料，不参与当前主线实验。

## 2. 当前本地材料

| 文件/目录 | 状态 | 处理方式 |
|---|---|---|
| `最后确认文件.md` | 当前主线依据 | 保留并作为总入口 |
| `项目分阶段执行计划与测试.md` | 当前推进计划 | 已同步阶段 0 |
| `架构概览.md` | 当前架构说明 | 保留 |
| `agent.md` | 当前 agent 规则 | 保留 |
| `参考论文详细解读_YUV可逆对抗攻击.md` | 当前论文解读 | 保留 |
| `Paper信息_阶段0.md` | 当前阶段 0 Paper 信息 | 已更新为 YUV 项目 |
| `Rt-LRM-main/` | 历史候选代码 | 不用于当前阶段 |
| `参考论文详细解读_Rt-LRM.md` | 历史候选解读 | 保留但不混用 |
| `YUV_Reversible_Attack_2025/` | 已存在 | 阶段 1 继续补充来源记录并确认目录 |

## 3. 本地与远程代码结构核验

本地 `YUV_Reversible_Attack_2025` 与 GitHub 页面显示的远程仓库结构一致，包含：

- `ORI_IMG`
- `pytorch_grad_cam`
- `EnModel.py`
- `LICENSE`
- `README.md`
- `atk.py`
- `calMetrics.py`
- `embed_utils.py`
- `helpers.py`
- `reversible_attack_OURS.py`
- `utils.py`

远程仓库 README 较简略，只有项目标题。因此本地复现将依赖本项目文档补齐环境、输入输出和测试流程。

当前本地附加状态：

- `ORI_IMG` 中已有 1000 张样例图。
- `output` 目录已存在。
- `runs` 目录已存在。
- `.venv` 目录已存在，但阶段 0 未测试环境可用性。
- 目录中无 `.git`，无法直接记录 commit hash。

## 4. 阶段边界

阶段 0 已完成：

- 主线确认。
- 材料来源确认。
- 旧材料隔离。
- 目标代码目录名确认。
- 本地代码目录和远程仓库结构确认。
- Paper 信息阶段 0 文档生成。

阶段 0 未完成也不应完成：

- 不安装依赖。
- 不运行攻击脚本。
- 不生成 RAE。
- 不报告攻击成功率、PSNR、SSIM、恢复误差等数值结果。

## 5. 下一步

进入阶段 1：获取代码与建立目录。

阶段 1 的最小验收目标是：

- `D:\大模型论文复现\YUV_Reversible_Attack_2025` 存在，当前已满足。
- 顶层文件与远程仓库结构一致，当前已满足。
- `ORI_IMG` 存在，当前已满足。
- `output\rae` 和 `runs` 已创建，当前已有 `output` 和 `runs`，阶段 1 需确认 `output\rae`。
- 记录代码获取方式；若没有 commit hash，则记录无 `.git` 和文件时间戳。
